#include <iostream>
#include <vector>
#include <map>
#include <string>
#include "libalg.h"

class Mesh {
    public:
        bool export(std::string filename);
        bool add_quad(double3 
    
    protected:
    
};